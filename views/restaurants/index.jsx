const React = require('react');
const Default = require('../default');

function index_page() {
	return (
		<Default>
			<main>
				<h1>INDEX PAGE</h1>
			</main>
		</Default>
	);
}

module.exports = index_page;
