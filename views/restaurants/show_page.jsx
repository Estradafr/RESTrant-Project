const React = require('react');
const Default = require('../default');

function show_page() {
	return (
		<Default>
			<main>
				<h1>SHOW PAGE</h1>
			</main>
		</Default>
	);
}

module.exports = show_page;
